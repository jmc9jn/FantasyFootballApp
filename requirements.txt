# Django backend
Django>=4.2,<5.0
django-extensions

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# AWS integration
boto3>=1.28.0
botocore>=1.31.0

# Excel file support (used in Lambda / ETL scripts)
openpyxl>=3.1.0

# Database drivers (optional depending on your setup)
psycopg2-binary>=2.9.0

# Utilities (safe for ETL pipelines)
python-dotenv>=1.0.0
